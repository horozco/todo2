= simple_form_for(list) do |f|
  - if list.errors.any?
    #error_explanation
      %h2
        = pluralize(list.errors.count, "error")
        prohibited this list from being saved:
      %ul
        - list.errors.full_messages.each do |msg|
          %li= msg
  = f.input :title
  = f.input :description , :input_html => {:rows => 8}
  = f.input :category_id, collection: Category.all, :prompt => "Select a Category", label_method: :get_name
  .form-actions
    = f.button :submit, class: "btn btn-primary"